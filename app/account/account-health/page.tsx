"use client";

// Mirror of /account-health under the account section, so users find it
// from /account/ links and the sidebar without remembering a separate path.
export { default } from "../../account-health/page";
