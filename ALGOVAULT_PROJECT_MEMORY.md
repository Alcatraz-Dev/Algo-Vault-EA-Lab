# AlgoVault — Project Memory (Authoritative)

**Generated:** 2026-09-22 · **Owner:** Haythem Dhahri · **Repo root:** `/Users/haythem_dhahri/Desktop/projects/website/trading-platform`
**Scope:** AlgoVault (Next.js 16.3.4 trading intelligence platform: strategy lab, signals, AI, plugins/EA ecosystem, copy trading, subscriptions).

> This is the single source of truth for agents working in this codebase. Read it before writing code. It supersedes all agent-instruction `.md` files EXCEPT `ALGOVAULT_AGENT_CONSTITUTION.md` (the product constitution, kept as canon). If this file and another doc conflict, this file wins; if this file and the constitution conflict, ask the user.
>
> **Agent Memory Rule:** After each session, update this file so the next agent inherits the full context. Never let stale memory survive: when a section no longer matches reality, fix it in the same session.

---

## §A. Project Summary

AlgoVault is a production-intent traffic trading platform combining:
- **Strategy Lab** — backtest engine (EA Lab), walk-forward, Monte Carlo, equity curve, report generator, strategy marketplace.
- **AI layer** — a canonical multi-provider AI Router (`lib/ai/router.ts`) with dynamic model discovery, free-only guard, local heuristic fallback. Providers: Gemini, OpenRouter, OpenCode, B.AI, **Bytez** (see §K).
- **Signals** — market scanner, AI-signal monitor (`lib/ai-signals/`), Telegram signal ingestion + delivery (`features/telegram-signals/`).
- **Market data** — TwelveData (primary REST), TradingView (live), RealMarket API, Biquote realtime.
- **Payments/EA ecosystem** — Stripe (subscriptions + Connect), MQL5 gateway EAs, licenses, plugins runtime with Vercel cron tick, copy trading.
- **Admin** — full admin section, Telegram/Discord delivery, notifications, usage analytics.

**Stack:** Next.js 16.3.4 (App Router, React 19.2.8, Tailwind v4, TypeScript 5), Firebase (RTDB only — **no Firestore**), Stripe, Telegram MTProto, MQL5.

**Ground truth for tooling:** This Next.js version has breaking changes vs. training data — read `node_modules/next/dist/docs/` (resolved from the repo root) before writing Next code. `AGENTS.md` is auto-regenerated by `next dev`; do not remove that block from diffs.

---

## §B. Repository Layout (top level)

```
app/                 # ~150 pages (App Router) + app/api (~230 route.ts)
components/          # UI: charts, trading, signals, admin, storefront, etc.
lib/                 # Domain logic (see §D) — the "brains"
features/            # feature/telegram-signals (ingestion+gateway+analytics+tests)
MQL5/                # Gateway EA + strategy-lab EA source (real, v1.3.0)
chrome-extension/    # TradingView browser extension (separate mini-project)
docs/                # Legit product docs (bot-integration-report, stripe-connect,
                     #   copy-trading-technical-spec, plugins-ecosystem)
scripts/             # Ops scripts (may contain stale/dead code — see §Y)
private-files/       # signer certs etc. (do not commit secrets)
```

**Junk dirs (excluded from builds/analysis, present on disk):** `.kilo/`, `graphify-out/`, `marketing-video/`, `.impeccable/`, `chrome-extension/dist/`. **Do not** treat these as source of truth; eslint must ignore them (see §Z).

---

## §C. Documentation Inventory (post-reset state)

| File | Status | Notes |
|---|---|---|
| `ALGOVAULT_PROJECT_MEMORY.md` | **THIS FILE — canon** | Single source of truth. |
| `ALGOVAULT_AGENT_CONSTITUTION.md` | **KEEP — canon** | 1618 lines; includes §54 design tokens; UI sweeps must add ZERO new lint errors. |
| `PRODUCT.md` | KEEP | Product direction. |
| `DESIGN.md` | KEEP | UI rules. |
| `TELEGRAM_CHANNELS_SETUP.md` | KEEP | Telegram channel setup guide. |
| `README.md` | KEEP (stale create-next-app boilerplate) | Update if touched; not authoritative. |
| `CLAUDE.md` | KEEP | Only `@AGENTS.md`. |
| `AGENTS.md` | AUTO-GENERATED | Next.js agent rules block; do not delete. |
| `realmarketapi-skills-catalog.SKILL.md` | KEEP | Skills catalog for RealMarket integration. |
| `docs/*.md` | KEEP | bot-integration-report, stripe-connect, copy-trading-technical-spec, plugins-ecosystem — legit product specs. |
| `.env.exemple` | ARCHIVED → `.env.example` | Misspelled template; replaced by committed `.env.example` (same vars + `BYTEZ_API_KEY`). |
| Root junk files | REMOVED | `check-firebase.mjs`, `start-monitoring.mjs`, `test-parser.ts`, `firebase-debug.log`, `settings.dat`, `msedgewebview2.dxvk-cache`, `scripts/tmp-dispatch.ts`, `scripts/diag-final.cjs`. |

Rule going forward: any new agent-instruction `.md` must either (a) be folded into this file, or (b) be a short pointer to a section here. No competing memory docs.

---

## §D. Domain Map — Canonical Implementation per Domain

One canonical system per domain (audit contract). When adding a feature, extend these modules — never fork a second implementation.

| Domain | Canonical location | Notes |
|---|---|---|
| AI Router | `lib/ai/router.ts` + `lib/ai/providers/*` | Bytez added here (see §K). |
| Market data | `lib/market-data/` (twelvedata, tradingview-live, cache, validation) | TwelveData primary; Biquote realtime used by signals. |
| Strategy lab | `lib/strategy-lab/` (engine, ea, backtest, report, marketplace helpers) | EA Lab tests: `npm run test:ea` (jiti runner). |
| Signals | `lib/ai-signals/` (monitor, engine, quality-filter, lifecycle) + `app/api/signals/*` | Deterministic signals from Biquote OHLC + Xoomar sentiment. |
| Telegram | `features/telegram-signals/` (gateway, generator, ingestion, analytics) | User-alert bot + admin ingestion via MTProto. |
| Risk | `lib/risk/` | Position sizing / risk checks + tests. |
| Gateway | `lib/gateway.ts` + `MQL5/` EAs | MQL5 gateway real v1.3.0; `GATEWAY_SECRET` auth. |
| Licensing | `lib/plugins/licensing.ts` + `app/api/license/*` + `app/api/performance/*` | EA-side license validation (public by design). |
| Notifications | `lib/notifications.ts`, `lib/email.ts`, `lib/telegram-monitoring.ts` | Resend or SendGrid (EMAIL_FROM verified sender). |
| Plugins ecosystem | `lib/plugins/` (catalog, runtime, licensing, permissions, ai-generator, seed) | Vercel cron `/api/plugins/runtime/tick` every minute; `PLUGIN_RUNTIME_CRON_SECRET`. |
| Copy trading | `lib/copy-trading.ts` | Execution tickets must be real from gateway, never `Math.random()`. |
| Auth/admin | `lib/admin-auth.ts` (`requireAdmin`, `authenticate`, `requireAdminOrProductOwner`), `lib/api-key-auth.ts` (`validateApiKey`), `lib/plugins/api-helpers.ts` (`authUid`) | See §O for route auth matrix. |
| Storage | Firebase RTDB only | **No Firestore.** Tables per `database.rules.json`. |
| UI shell | `components/layout/*` (AppShell/AdminShell own `data-guide="page-header"`) | Exception: `/trading` is standalone (owns its own AccountHeader page-header). |

---

## §E. Firestore-Deprecated / RTDB-Only Rule

- **RTDB is the ONLY database.** Never introduce Firestore. `firebase-admin.ts` connects to RTDB (`NEXT_PUBLIC_FIREBASE_DATABASE_URL`).
- `database.rules.json` is the RTDB ruleset — it is committed and read fully during audits. Rules exist for all domains: users, portfolios, signals, subscriptions, licenses, plugins, telegram, notifications, copy-trading, etc.
- Known rule gaps (documented, not changed without user sign-off):
  - `users` has `indexOn` only for subscription id fields.
  - `products` / `reviews` / `social_feed` / `pluginReviews` allow authenticated-user writes (public community content — intentional).
- `.firebaserc` + `firebase.json` exist (emulator config). `firebase-debug.log` is junk → gitignored.

---

## §F. Authentication & Authorization

- **Server helpers** (`lib/admin-auth.ts`): `requireAdmin`, `authenticate`, `requireAdminOrProductOwner` — used by most admin/api routes.
- **API key auth** (`lib/api-key-auth.ts`): `validateApiKey` for third-party/extension keys.
- **Plugin helpers** (`lib/plugins/api-helpers.ts`): `authUid` for plugin runtime routes; `PLUGIN_RUNTIME_CRON_SECRET` for cron.
- **Public-by-design** (no auth): home/marketing, economic calendar, market data reads, webhooks (Stripe/TradingView, secret-gated), EA-side license checks (`/api/license/validate`, `/api/performance/*`).
- **Route inventory:** ~230 routes inventoried; auth matrix documented in audit. **Security fixes applied:** (1) `app/api/ai-signals/monitor/route.ts` now gated with `x-cron-secret`/`CRON_SECRET`; (2) `app/api/extensions/analysis/route.ts` now validates `x-api-key` via `validateApiKey` (was checking presence only); (3) `app/api/alerts/check/route.ts` and `app/api/trade-management/monitor/route.ts` were **fail-OPEN** (`secret !== CRON_SECRET` authorized when CRON_SECRET unset) — now fail-closed with the canonical `x-cron-secret`/`?secret=`/vercel-cron pattern (2026-09-22); (4) `app/api/telegram-signals/webhook/route.ts` had a **hardcoded fallback secret `"default_ingest_secret"`** + a `NODE_ENV !== "production"` bypass — hardcoded default removed (fail-closed when `TELEGRAM_INGESTION_SECRET` unset) and the dev bypass is now explicit via `TELEGRAM_INGESTION_ALLOW_DEV=1` (2026-09-22). Keep them gated forever.

---

## §G. Environment & Secrets

- Committed template: **`.env.example`** (new). Local: `.env.local` (real values, gitignored). `.env*` is gitignored; `.gitignore` has `!.env.example` to allow committing the template.
- All secrets are server-side. `NEXT_PUBLIC_*` = client-safe (Firebase config, app URL, bot username, Discord client id).
- **Fail-closed rule:** env accessors in `lib/ai/config.ts` (and everywhere) must never crash when keys are missing — `isAvailable()` returns false and the router falls through to the next provider/local heuristic.
- Secret inventory (see `.env.example`): Firebase (client+admin), Stripe (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_V2_WEBHOOK_SECRET`, `STRIPE_SUBSCRIPTION_PRICE_ID`, `STRIPE_CONNECT_URL`, `NEXT_PUBLIC_APP_URL`), Telegram, Discord, Resend/SendGrid (`EMAIL_FROM`), `TWELVE_DATA_API_KEY`, `TRADINGVIEW_WEBHOOK_SECRET`, `GATEWAY_SECRET`, **`CRON_SECRET`** (scheduled + monitor endpoints; fail-closed when unset), **`TELEGRAM_INGESTION_SECRET`** (+ dev opt-in `TELEGRAM_INGESTION_ALLOW_DEV`), AI (`AI_FREE_ONLY`, `AI_DEFAULT_PROVIDER`, `AI_DEFAULT_MODEL`, `AI_MAX_PROVIDER_ATTEMPTS`, `AI_REQUEST_TIMEOUT_MS`, per-provider keys), `REALMARKET_API_KEY`, `PLUGIN_RUNTIME_CRON_SECRET`, **`BYTEZ_API_KEY`**.
- Default `AI_FREE_ONLY=true` — the free-only guard is enforced by default.
- Fail-closed rules: cron/monitor/ingestion endpoints refuse to run when their secret is unset rather than accepting anonymous calls (verified 2026-09-22 for all three cron routes, the three monitors, and the telegram ingestion webhook).

---

## §H. Data Layer — Firebase

- Client: `lib/firebase.ts` (web SDK). Server: `lib/firebase-admin.ts` (admin SDK, `firebase-admin` v14).
- RTDB paths (canonical set — see `database.rules.json` for exact rules): users, subscriptions, portfolios, trades, signals (history/stats/usage), ai-signals (monitor state, stats), products, reviews, plugins (catalog/runtime), licenses, telegram (channels, delivery), notifications, copy-trading, performance, settings.
- Watch out: RTDB reads/writes are untrusted inputs; validate before use.

---

## §I. Routing & Pages (Inventory Summary)

- ~150 `page.tsx` across `app/` sections: account, account-health, admin (intelligence, plugins, extensions, telegram, etc.), affiliates, ai-copilot, ai-historical, alert-center, alerts, analysis, backtests, bots, broker-compare, compare, copy-trading, correlation, dashboard, developer, donate, economic-calendar, equity-curve, execution-analytics, goals, insights, live, login, marketplace, monte-carlo, news, portfolio, pricing, privacy, register, refunds, report-generator, risk, scanner, signal-transparency, signals (feed/pro/stats), social, spreads, statement, store, strategy-compare, strategy-lab, tags, terms, tools, trade-journal, trade-management, trade-replay, trading (standalone terminal), verified-performance, walk-forward.
- `app/l`, `app/legal`, `app/rankings`, `app/bots` were **empty dirs — removed**. `app/admin/intelligence/{sandbox,studio}` empty dirs removed.
- `/trading` is a standalone terminal (owns its own `AccountHeader` `data-guide="page-header"`, no AppShell). All other pages use AppShell/AdminShell which own the page header.
- Page→API mapping table was built during audit (see repo `app/api` for handlers; every page's data is backed by a canonical route listed there).

---

## §J. API Routes (Inventory Summary)

- ~230 `route.ts` handlers organized by domain under `app/api/*`.
- Cron jobs in Vercel (`vercel.json`): `/api/signals/auto-update` every 5 min, `/api/pro-signals/auto-update` every 5 min, `/api/plugins/runtime/tick` every minute.
- **Cron secret naming is inconsistent** across routes (historical): `CRON_SECRET` used by `<some>` routes; `PLUGIN_RUNTIME_CRON_SECRET` used by plugin cron. When touching a cron route, standardize on the env var actually gating it and document here. `ai-signals/monitor`, `alerts/check`, `trade-management/monitor` have NO cron entries in `vercel.json` — verified 2026-09-22 that they are **fail-closed and callable-with-secret only** (external schedulers may hit them; not scheduled by Vercel). `app/api/telegram-signals/webhook/route.ts` is also **fail-closed** (`TELEGRAM_INGESTION_SECRET`, dev bypass opt-in via `TELEGRAM_INGESTION_ALLOW_DEV=1`).
- **Signals cron auth (verified):** `/api/signals/auto-update` accepts `x-cron-secret` matching `CRON_SECRET` OR user-agent `vercel-cron`; refuses (401) if `CRON_SECRET` is unset. `/api/pro-signals/auto-update` accepts vercel-cron UA OR `x-cron-secret`/`?secret=CRON_SECRET` OR `Authorization: Bearer <Firebase idToken>` (the pro page's "Scan now" sends the bearer). `vercel.json` 5-min crons send no auth headers → they rely on the vercel-cron UA path, which both routes honor.

---

## §K. AI Router & Bytez Integration (canonical)

### Architecture
`lib/ai/router.ts` (class `AIRouter` + exported `defaultRouter`) manages a `Map<providerId, AIProvider>`. Fixed fallback order: Gemini → OpenRouter → OpenCode → B.AI → **Bytez** → Local heuristic (`lib/ai/local.ts`). Free-only guard `assertFreeModelAllowed` in `lib/ai/models.ts` blocks paid models unless a paid budget is allowed. Dynamic model discovery per provider via `getModels()`; `getAllModels()` aggregates and falls back to `KNOWN_FREE_MODELS` on total failure. Bytez joins the chain whenever `BYTEZ_API_KEY` is configured: under `AI_FREE_ONLY=true` (default) only `meter`-`*-free` models are served, and when a paid budget is allowed (`AI_FREE_ONLY=false`) its chat default is the first enabled discovered model (never a hardcoded catalog). `getAIProvider()` mirrors the provider chain (gemini → openrouter → opencode → bai → bytez).

### Provider interface (implement in `lib/ai/providers/*`)
`id`, `name`, `isAvailable()`, `getModels(): Promise<AIModel[]>`, `chat(req): Promise<AIResponse>`, plus optional `describeStrategy`/`summarizeAnalysis`. Register in `AIRouter` constructor and in `getAvailableCloudProviders()` (and ensure `getAllModels()` covers it — it iterates `this.providers.values()`, so constructor registration is enough).

### Bytez provider (`lib/ai/providers/bytez.ts`) — the real integration
- Base URL: `https://api.bytez.com`. Chat: `POST /models/v2/openai/v1/chat/completions` (OpenAI-compatible: `{model, messages:[{role,content}], max_completion_tokens, temperature}`).
- **Auth header is `Authorization: <BYTEZ_API_KEY>` RAW — no `Bearer` prefix.** 401 → `INVALID_API_KEY`, **402 → `QUOTA_EXCEEDED`** (credit exhaustion), 429 → `RATE_LIMITED`.
- **Dynamic discovery (never hardcode model lists):** `GET /models/v2/list/tasks` → `{error, output: string[]}`; `GET /models/v2/list/models?task=<task>` → `{error, output: [{meter, meterPrice, modelId, params, ramRequired, task}]}`. Use tasks to pick the right catalog (chat, code, etc.).
- **Free tier honesty:** Bytez is credit-based, NOT free. `meter` ending `-free` = free-eligible. Everything else is paid/metered and MUST be blocked when `AI_FREE_ONLY=true`. Do not label Bytez models "free" in the UI unless `meter` ends in `-free` (verified).
- `isAvailable()` returns true only when `BYTEZ_API_KEY` is set (server-side only; never hardcoded; never crash when missing).
- `.env.example` documents `BYTEZ_API_KEY`.

### Reference provider template
`lib/ai/providers/openrouter.ts` is the closest template (error mapping, timeout via `AIConfig.timeoutMs`, finish-reason mapping in `lib/ai/finish.ts`).

---

## §L. Signals System (canonical)

- `lib/ai-signals/` — monitor, engine, quality-filter, lifecycle-engine, stale-detection, price-sanity, confidence, tiers, sessions-filter, symbol-specs, freshness-guard, access, notify, events, analytics, statistics, results.
- Signals are computed deterministically from Biquote OHLC + Xoomar sentiment (news), then quality-filtered — **no random directions**. The old `Math.random()` direction in `features/telegram-signals/generator/ai-signal-generator.ts` was replaced with a deterministic rule. Do not reintroduce randomness.
- API: `/api/signals/*` (feed, stats, history, execute, auto-update), `/api/pro-signals/*`, `/api/ai-signals/*` (monitor gated with cron secret; main route protected).
- Execution tickets: real gateway-provided tickets only. The fake `#EXE-<random>` tickets in `lib/copy-trading.ts`, `features/telegram-signals/gateway/gateway-adapter.ts`, and `app/api/signals/execute/route.ts` were replaced with honest "submitted/pending" states and real gateway tickets. If a path still fakes a ticket, fix it.
- Price sanity (`lib/ai-signals/price-sanity.ts`): per-instrument % thresholds against the live quote. FX majors use **0.05%** (≈5 pips on EURUSD). Do not tighten FX back to 0.01% — that rejects nearly every real signal (entry prices naturally differ by a few pips from the live quote).
- Tests: `lib/ai-signals/__tests__/run-ai-signals-tests.ts` (price sanity, lifecycle, monitor) — run via `node scripts/jiti-tsrun.mjs` (needs tsconfig `@/` path + env resolution).

---

## §M. Telegram Features (canonical)

- `features/telegram-signals/` — gateway (signals → channel(s)), generator (AI-assisted signal creation), ingestion (MTProto admin client via `TELEGRAM_API_ID/HASH`), RealtimeFeed component, analytics, lifecycle, tests.
- `TELEGRAM_CHANNELS_SETUP.md` documents channel setup. User alerts bot (token) vs. admin ingestion client are separate credentials — keep them separate.
- Tests: `features/telegram-signals/tests/run-tests.ts` (custom runner).

---

## §N. Plugins & EA Ecosystem (canonical)

- `lib/plugins/` — catalog, database, licensing, manifest, permissions, runtime (conditions, tick), ai-generator, api-helpers, seed, types, ui.
- Admin: `app/admin/plugins/*` (+ `app/admin/extensions/*`). Runtime: `app/api/plugins/*`. Cron: `/api/plugins/runtime/tick`.
- Licensing: EA-side `/api/license/validate` + `/api/performance/*` validate EA signatures — public by design (EAs hold no API keys; the license check is the security boundary).
- `app/api/extensions/analysis/route.ts` validates `x-api-key` (fixed). Any new third-party endpoint must use `validateApiKey` from `lib/api-key-auth.ts`.
- AI Plugin Studio uses the shared AI router (respects `AI_FREE_ONLY`); prefer a paid provider key in production so drafts aren't throttled by the free tier.

---

## §O. Security Posture

- **Auth-first:** every new API route must authenticate (admin, user, API key, or webhook secret). If a route must be public, make that a deliberate, commented decision.
- **Never** log secrets; never store API keys client-side beyond `NEXT_PUBLIC_*` whitelist; never put `BYTEZ_API_KEY`/`GATEWAY_SECRET`/Stripe keys in client bundles.
- **Path traversal:** `backtests/report` etc. use safe path resolution (verified during audit).
- **Webhooks:** Stripe (thin events → `STRIPE_V2_WEBHOOK_SECRET` optional), TradingView (`TRADINGVIEW_WEBHOOK_SECRET`), Telegram — verify signatures/secrets.
- **Cron/secret mismatch** (§J) — standardize when touched.
- **`extensions/analysis`** and **`ai-signals/monitor`** are now gated (see §F). Do not regress.

---

## §P. Testing Strategy & Runners

Repo has **no vitest dependency** — tests use custom jiti-driven runners. **Do not convert to vitest**; extend the existing patterns.

**Canonical runner for `@/`-import suites:** `node scripts/jiti-tsrun.mjs <runner.ts>` (loads tsconfig `@/` aliases + env). Plain `jiti <runner.ts>` only works for suites that avoid `@/` imports (e.g. EA tests). `require.main === module` gates break under `jiti.import` — runners self-execute via a top-level call (see `lib/risk/tests/run-tests.ts`).

| Suite | Command | Status |
|---|---|---|
| EA Lab | `npm run test:ea` (jiti `lib/strategy-lab/ea/tests/run-tests.ts`) | ✅ PASSING |
| Strategy Lab backtest | `node scripts/jiti-tsrun.mjs lib/strategy-lab/tests/run-tests.ts` | ✅ PASSING (100%) — was failing at HEAD; fixed (see §AH) |
| Telegram / pro signals | `node scripts/jiti-tsrun.mjs features/telegram-signals/tests/run-tests.ts` | ✅ PASSING (100%) |
| Risk | `node scripts/jiti-tsrun.mjs lib/risk/tests/run-tests.ts` | ✅ PASSING (100%) |
| AI-signals | `node scripts/jiti-tsrun.mjs lib/ai-signals/__tests__/run-ai-signals-tests.ts` | ✅ PASSING (100%) |
| Agents | `node scripts/jiti-tsrun.mjs lib/agents/__tests__/run-agents-tests.ts` | ✅ PASSING (100%) |
| Chrome extension | `cd chrome-extension && npm run typecheck` | ✅ PASSING |

**Verification baseline (green as of 2026-09-22, production-hardening pass):** `npx tsc --noEmit` (clean), `npm run lint` (906 problems: 294 errors / 612 warnings — down from 434; zero errors in files touched this session), `npm run build` (exit 0), **all 8 suites above pass** (incl. strategy-lab backtest suite, newly green), chrome-extension typecheck. CI: `.github/workflows/ci.yml` (tsc + lint (continue-on-error) + build (secret-gated) + all suites + extension typecheck).

---

## §Q. Known Type Errors (fixed during this audit)

Recorded as fixed; if they reappear, the pattern is known:
- `lib/agents/index.ts` re-export ambiguity (TS2308) — dedupe exports.
- `lib/ai-signals/types.ts` TS2717 conflicting declarations + missing `Timeframe` export; `engine.ts:37` `@/ai` unresolved import → fixed to relative import of canonical AI router.
- `lib/market-data/twelvedata/symbol-map.ts` `AUDCNH` TS2353 → corrected mapping.
- Telegram-signals type errors (gateway adapter etc.).
- `chrome-extension` `chrome` globals → added ambient types.
- `lib/agents/permissions.ts` TS7053 index signature.
- `scripts/tmp-dispatch.ts` had `import type` of missing module — removed (junk script).
- `lib/agents/database.ts` — Firebase RTDB `.set/.update` reject nested `undefined`; every agent write now deep-converts `undefined → null` (evidence `value`, optional `user` fields). Do not remove the boundary sanitizer.
- `lib/agents/__tests__/workflow-engine.test.ts` — engine's workflow-level permission gate threw for `basicWorkflow` (required `market_data`); tests now pass `permissions: { market_data, notifications }` via `testConfig()`. Missing agents and permission-denied steps now store a per-step failed `output` (was missing for sequential steps).
- `lib/agents/catalog.ts` — synthesis agent id unified to **`synthesis`** (executor, `notification-agent`, engine `finalOutput` and tests already used it; the catalog's `synthesizer` id was the outlier). `wfAgentForPermission` `ai_analysis` → `synthesis`. Its contract requires `market_data` + `notifications`.
- `lib/ai-signals/price-sanity.ts` — FX-major thresholds raised 0.01% → 0.05% (see §L).

---

## §R. Lint Debt (honest status)

- Baseline: ~6243 problems (~1337 errors, ~4907 warnings) across ~997 files — but **~half is junk worktrees** (`.kilo/**`, `chrome-extension/dist/**`, `graphify-out/**`, `marketing-video/**`, `.impeccable/**`) that eslint did not ignore.
- After adding globalIgnores for those dirs, remaining debt is mostly `react-hooks/*`, `no-unescaped-entities`, `@typescript-eslint/no-explicit-any` across `app/api`, admin pages, and scripts.
- Current count: **906 problems (294 errors, 612 warnings)** as of 2026-09-22 (down from ~6243; prior audit pass: 1049/434). This hardening pass fixed the remaining 3 errors in the three routes it touched (`alerts/check` biquote-bars `any`, `telegram-signals/webhook` `catch any`) plus 137 earlier (→ 297 → 294). Files touched during the audit are at **zero errors** (warnings only), per constitution §54.8.
- Constitution §54.8 records lint debt as pre-existing and REQUIRES UI sweeps to add **zero new errors**. Honor that.
- Do NOT mass-disable rules; fix code, or add targeted, justified overrides.

---

## §S. UI/UX Rules (from DESIGN.md + Constitution §54)

- Follow `components.json` (shadcn-style `components/ui/*`), base-ui/radix primitives, Tailwind v4. Design tokens in Constitution §54.
- Fidelity first: Dashboard, trading, and admin screens must look production-grade — no demo placeholders.
- Charts use `lightweight-charts` v5 (and TradingView embed where present). **No random/decorative candles**: `GoChartingChart.tsx`, `TradingChart.tsx`, `SignalChart.tsx` previously rendered random candles — replaced with real/empty states. Same for `ProTools.tsx` fake profit/drawdown/winRate/score metrics — show real data or an honest "no data".
- Page header: AppShell/AdminShell own it everywhere except `/trading`.

---

## §T. Stripe Integration

- `lib/stripe.ts`, `lib/stripe-connect-utils.ts`, `lib/subscription-server.ts`, `lib/subscription.ts`; webhook at `app/api/webhooks/*`.
- Subscriptions + Store checkout; Connect for marketplace/vendor payouts; `STRIPE_CONNECT_URL`.
- `docs/stripe-connect.md` = canonical Connect spec. Stripe skills available: `stripe-best-practices`, `connect-recommend`, `upgrade-stripe`, etc.
- Thin events: `STRIPE_V2_WEBHOOK_SECRET` optional; falls back to `STRIPE_WEBHOOK_SECRET`.

---

## §U. Copy Trading (canonical)

`lib/copy-trading.ts` is the single source for copy-trading logic. UI: `app/copy-trading/`, API under `app/api/copy-trading/*` (inventoried). Execution must go through the real MQL5 gateway — ticket numbers come from the gateway response, never synthesized. Public transparency page exists (`app/copy-trading`).

---

## §V. Marketplace & Store

`app/marketplace`, `app/store`, `components/storefront/*`, `lib/reviews.ts`, `lib/product-files.ts`; RTDB paths `products`, `reviews`. Community content rules allow authenticated writes. `app/developer` for developer onboarding.

---

## §W. Nostr / Social / Misc

`app/social` exists (community feed). Unrelated: don't invent new social infra; extend `app/social` + RTDB `social_feed`.

---

## §X. Deploy & Ops (Vercel + Firebase)

- `vercel.json`: 3 cron jobs (see §J). `next.config.ts` minimal. Node runtime default; Firebase admin initialized server-side with env creds.
- Build pipeline: `npm run build` (must pass), `npm run lint`, `npx tsc --noEmit`.
- CI: **`.github/workflows/ci.yml`** added 2026-09-22 — job `validate` (Node 26: `npm ci` → `tsc --noEmit` → `eslint` (continue-on-error, enforcing against pre-existing debt §54.8) → EA, strategy-lab, ai-signals, risk, agents, telegram pro-signals suites → chrome-extension typecheck) + job `build` (`next build`, gated on `FIREBASE_PROJECT_ID` secret availability since `lib/firebase-admin.ts` throws at module scope without env).

---

## §Y. Dead Code & Junk Cleanup (done)

Removed/archived: `scripts/tmp-dispatch.ts`, `scripts/signal-auto-update.js`, root `test-parser.ts`, `check-firebase.mjs`, `start-monitoring.mjs`, `firebase-debug.log`, `settings.dat`, `msedgewebview2.dxvk-cache`, empty dirs (`app/l`, `app/legal`, `app/rankings`, `app/bots`, `app/admin/intelligence/{sandbox,studio}`), `chrome-extension/probe-*.mjs`. `.gitignore` updated to cover junk and allow `.env.example`.
**Kept but eslint-ignored (tracked diagnostic/vendor files, NOT junk):** `scripts/diag-final.cjs`, `scripts/check_db.mjs`, `public/account/vendor/html2canvas.min.js` — see §Z.

---

## §Z. Tooling Config Notes

- `eslint.config.mjs` globalIgnores: `.kilo/**`, `chrome-extension/dist/**`, `graphify-out/**`, `marketing-video/**`, `.impeccable/**` (in addition to defaults `.next/**`, `out/**`, `build/**`, `next-env.d.ts`), plus vendored/diagnostic noise `public/account/vendor/html2canvas.min.js`, `scripts/diag-final.cjs`, `scripts/check_db.mjs`. Do not un-ignore these without a reason.
- Test runner glue: `scripts/jiti-tsrun.mjs` (tsconfigPaths + env/vars loading) is the canonical way to run `@/`-import suites (see §P).
- `.gitignore`: `.env*` + `!.env.example`, `/.kilo/`, `graphify-out/`, `*.tsbuildinfo`, `next-env.d.ts`, firebase-debug.log, `settings.dat`, `msedgewebview2.dxvk-cache`.
- `tsconfig.json` standard Next; `next-env.d.ts` gitignored (auto-generated).
- Types: `types/` global dir present (empty); ambient `chrome` types live in `chrome-extension/`.

---

## §AA. Agents Layer (canonical)

`lib/agents/` — catalog (13 built-in agents, mostly deterministic), orchestrator, workflow-engine, ai-provider (uses the shared AI Router — NOT its own LLM client), database (RTDB), permissions + permissions-runtime, schema, types, notification-agent. UI: `app/admin/intelligence/*`. API: `app/api/agents/*`, `app/api/admin/agents/*`. Tests: `lib/agents/__tests__/run-agents-tests.ts` (jiti, see §P). Re-export ambiguity in `lib/agents/index.ts` was fixed.
- **Synthesis agent id is `synthesis`** (catalog id unified from `synthesizer`; executor registered as `synthesis`; `notification-agent` and engine `finalOutput` expect it). Requires permissions `market_data` + `notifications`.
- **Per-step outputs are always stored**: unknown agents and permission-denied sequential steps write a failed `outputs[step.id]` (matches the parallel path) so consumers always find a per-step record.
- **DB writes sanitize `undefined → null`** at the RTDB boundary (`lib/agents/database.ts` `REF` wrapper + `deepClean`) — Firebase rejects nested `undefined` (e.g. `evidence.value`, optional `user` fields).
- Workflows/tests must pass explicit `permissions` in `EngineConfig` for permission-requiring workflows, or the workflow-level gate throws `Permission denied`.

---

## §AB. RealMarket API & Market Data

- `lib/realmarketapi/` + `realmarketapi-skills-catalog.SKILL.md` — realtime market prices catalog.
- `lib/market-data/` — `twelvedata` (primary REST + symbol-map), `tradingview-live.ts`, `live-stream.ts`, `cache.ts`, `market-truth.ts`, `normalizer.ts`, `validation.ts` (sanity checks).
- Biquote realtime feeds the signals engine. TwelveData handles OHLC. Keep the symbol maps in sync with providers — `symbol-map.ts` must be type-correct (fixed `AUDCNH`).

---

## §AC. Quality Bar (rules we do not break)

1. **No fake data** — no `Math.random()` for signals, prices, metrics, tickets, or analytics. Show real data or an honest empty/loading state.
2. **No duplicate systems** — one canonical implementation per domain (§D). Extend, don't fork.
3. **No Firestore** — RTDB only.
4. **AI Router only** — never spin up a second LLM client; always `lib/ai/index.ts` exports.
5. **Secrets server-side** — never `NEXT_PUBLIC_` a secret; commit only `.env.example`.
6. **Verification honest** — run tsc/lint/build/tests; report real failures, don't hide them.
7. **Zero new lint errors in UI sweeps** (Constitution §54.8).

---

## §AD. Frequently Hit Traps

- Next.js 16 API differences — read `node_modules/next/dist/docs/` first.
- `next dev` rewrites `AGENTS.md` — don't fight it; keep the block in commits.
- Telegram MTProto and Stripe thin events have version-specific payloads — check `docs/` and the skills before changing webhooks.
- Bytez auth header has NO `Bearer`; 402 means "insufficient credits" → map to `QUOTA_EXCEEDED`.
- `AI_FREE_ONLY=true` is the default; new providers must respect it from day one.
- jiti custom test runners, not vitest.

---

## §AE. Immediate Next Steps (open work)

1. ✅ Audit + inventory (this doc).
2. ✅ Documentation reset & junk cleanup (§Y).
3. ✅ TS error fixes (§Q) — re-verified with `npx tsc --noEmit` (clean).
4. ✅ Bytez provider + registration (§K) + config/router/index wiring + `.env.example`.
5. ✅ Security fixes (§F).
6. ✅ Fake-data fixes (§L/§U/§S).
7. ✅ **VERIFY** (2026-09-22): `npx tsc --noEmit` clean · `npm run lint` 906 problems (294 errors/612 warnings, zero in touched files) · `npm run build` exit 0 · **all 8 runnable suites green** (EA, strategy-lab backtest, pro/telegram signals, risk, ai-signals, agents) · chrome-extension typecheck clean.
8. ⏳ Right-size remaining lint: fix (not mass-disable) `react-hooks`/`any` errors in app/api + admin pages (**294 errors remain** — next batch candidates: admin/products/versions, admin/telegram/*, correlation, scanner/upload, backtests/live pages).
9. ✅ Add CI (GitHub Actions) — `.github/workflows/ci.yml` (tsc, lint, build, all suites, extension typecheck).
10. ✅ Cron monitors decision (2026-09-22): `ai-signals/monitor`, `alerts/check`, `trade-management/monitor` are **not** in `vercel.json`; left callable-with-secret only, now all fail-closed (2 were fail-open, fixed). Add to `vercel.json` only if they are meant to run periodically.
11. ✅ Signals cron auth (2026-09-22): `/api/signals/auto-update` + `/api/pro-signals/auto-update` gated (CRON_SECRET/vercel-cron UA, pro also accepts Firebase bearer; verified against `vercel.json`). See §J.
12. ✅ Telegram ingestion webhook hardened (2026-09-22): hardcoded `default_ingest_secret` removed; `TELEGRAM_INGESTION_SECRET` required; dev bypass now `TELEGRAM_INGESTION_ALLOW_DEV=1`.
13. ✅ Pine runtime backtest engine fixed (2026-09-22): named orders, builtin field access, cost model — see §AH. `STRIPE_SUBSCRIPTION_PRICE_ID` remains an unimplemented TODO (`.env.example`), existing checkout uses inline `price_data`.

---

## §AF. Future Agent Contract

When a new agent (human or AI) starts working on AlgoVault, it MUST:

1. **Read this file fully** (`ALGOVAULT_PROJECT_MEMORY.md`) and `ALGOVAULT_AGENT_CONSTITUTION.md` before touching code.
2. Read the current relevant section of `node_modules/next/dist/docs/` before writing any Next.js code (this Next version is NOT training-data Next).
3. Respect the **one canonical implementation per domain** rule (§D) — locate and extend the canonical module, never fork.
4. Respect **RTDB-only** (§E), **AI Router-only** (§K/§AC), **no fake data** (§AC), **secrets server-side** (§G/§O).
5. Verify with, at minimum: `npx tsc --noEmit`, `npm run lint`, `npm run build`, and all runnable test suites (§P). Report results honestly — include failures, don't hide them.
6. When adding a domain or significant feature, update this memory doc (§-mapped sections) in the SAME session.
7. When fixing a bug **because** of stale/missing memory, update the corresponding section so the fix is not rediscovered.
8. When asked to "clean up docs", never delete legit product docs (§C) without user confirmation; archive instead.
9. Never convert Bytez/other AI models to "free" labels without verified `meter` evidence (§K).
10. When adding API routes, add the auth decision (helper + why public if public) inline so future audits can verify (§F/§O).

---

## §AG. Agent Memory Rule

- This file is updated **after every working session** — preferably in the same PR/commit as the code change it describes.
- Prefer editing the exact bullet/section over rewriting the whole file; keep the lettered structure stable (A–AG) so cross-references stay valid.
- If a section becomes wrong, fix it now — stale memory is worse than no memory.
- Mark work done with ✅ and open work with ⏳ in §AE so the next agent sees where to start.

---

## §AH. Pine Runtime Backtest Engine — Fixed 2026-09-22 (was failing at HEAD)

`node scripts/jiti-tsrun.mjs lib/strategy-lab/tests/run-tests.ts` FAILED at HEAD (`5ec3f45`); now passes 100%. Three distinct root causes, all in `lib/pine-runtime/`:

1. **Named order ids were discarded** — `strategy.entry("L", …)` never stored the order name, so `strategy.close("L")`/`strategy.exit("L")` (which match by name) could never fire → 0 closed trades. Fix: `StrategyTrade.entryId?: string` (types.ts), set it in the `entry` handler, match it in `exit`/`close` alongside the existing `id.includes` fallback (runtime.ts).
2. **Builtin field-access objects were unresolvable** — `barstate.islast`/`syminfo.*`/`timeframe.*` went through the generic `FieldAccess` case, which resolved the base Identifier via scope lookup only → `undefined` on every bar → `if barstate.islast …` never ran. Fix: the `FieldAccess` case now routes `barstate`/`syminfo`/`timeframe` through the same `getDefaultBarState`/`getDefaultSymbolInfo`/`getDefaultTimeframeInfo` sources the namespaced-call path uses (runtime.ts).
3. **Pine cost model was not applied** — `backtestPine` copied `t.pnl` (gross) into `profit`/metrics without spread/slippage/commission. Fix (backtest.ts): per-trade `net = gross − unitCostPerUnit × size` where `unitCostPerUnit = spreadPips·pipSize + slippagePips·pipSize·2 + commissionPerLot`, using `SYMBOL_SPECS` (`@/lib/ai-signals/symbol-specs`) for pip size (XAUUSD 0.01). Mirrors the Strategy Lab engine's convention where `profit == pnlGross == net`; `spreadCost`/`commission`/`slippageCost` are now reported on the Pine path too. Note: the Pine path deliberately does NOT multiply by `contractSize` — the test contract (`backtest.test.ts` blocks 3 & 5) defines these unit costs per engine-size unit.

**Test correction (not engine hack):** block 5 asserted profit against `candles[9]` (a bar index copied from block 3's 10-candle window) on a 12-candle run where `barstate.islast` = bar 11 — physically unsatisfiable; corrected to `candles[candles.length - 1]` with a comment. Block 3 (10 candles, last = index 9) was already consistent and passes unchanged.